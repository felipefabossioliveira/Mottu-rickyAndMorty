import { CounterAnimation } from './counter-animation';

describe('CounterAnimation', () => {
  it('should create an instance', () => {
    const directive = new CounterAnimation();
    expect(directive).toBeTruthy();
  });
});
